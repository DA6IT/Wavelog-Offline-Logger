name: CI

on:
  push:
  pull_request:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  selftest:
    name: Python selftests
    runs-on: windows-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v7

      - name: Set up Python
        uses: actions/setup-python@v6
        with:
          python-version: '3.12.10'

      - name: Run selftests
        run: python selftest.py

  windows-build:
    name: Windows x64 build
    needs: selftest
    runs-on: windows-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v7

      - name: Set up Python
        uses: actions/setup-python@v6
        with:
          python-version: '3.12.10'

      - name: Set up Go
        uses: actions/setup-go@v7
        with:
          go-version: '1.23.2'
          cache: false

      - name: Build Windows executable
        shell: pwsh
        run: .\scripts\build-windows.ps1 -OutputDirectory dist -SkipTests

      - name: Upload build artifact
        uses: actions/upload-artifact@v7
        with:
          name: windows-x64
          path: |
            dist/*.exe
            dist/SHA256SUMS.txt
          if-no-files-found: error

  macos-build:
    name: macOS ${{ matrix.package_arch }} build
    strategy:
      fail-fast: false
      matrix:
        include:
          - runner: macos-15
            python_arch: arm64
            package_arch: arm64
          - runner: macos-15-intel
            python_arch: x64
            package_arch: x64
    runs-on: ${{ matrix.runner }}
    steps:
      - name: Checkout
        uses: actions/checkout@v7

      - name: Set up Python
        uses: actions/setup-python@v6
        with:
          python-version: '3.12.10'
          architecture: ${{ matrix.python_arch }}

      - name: Test and build app bundle
        shell: bash
        run: ./scripts/build-macos.sh dist

      - name: Upload macOS artifact
        uses: actions/upload-artifact@v7
        with:
          name: macos-${{ matrix.package_arch }}
          path: |
            dist/*.zip
            dist/*.sha256
          if-no-files-found: error

  linux-build:
    name: Linux ${{ matrix.package_arch }} packages
    needs: selftest
    strategy:
      fail-fast: false
      matrix:
        include:
          - runner: ubuntu-22.04
            python_arch: x64
            package_arch: x64
          - runner: ubuntu-22.04-arm
            python_arch: arm64
            package_arch: arm64
    runs-on: ${{ matrix.runner }}
    steps:
      - name: Checkout
        uses: actions/checkout@v7

      - name: Set up Python
        uses: actions/setup-python@v6
        with:
          python-version: '3.12.10'
          architecture: ${{ matrix.python_arch }}

      - name: Install Linux build dependencies
        shell: bash
        run: |
          sudo apt-get update
          sudo apt-get install -y build-essential curl dpkg-dev file python3-tk squashfs-tools zstd

      - name: Test and build Linux packages
        shell: bash
        run: bash ./scripts/build-linux.sh dist

      - name: Upload Linux artifacts
        uses: actions/upload-artifact@v7
        with:
          name: linux-${{ matrix.package_arch }}
          path: |
            dist/*.deb
            dist/*.AppImage
            dist/*.pkg.tar.zst
            dist/*.sha256
          if-no-files-found: error
